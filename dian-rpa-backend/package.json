{
  "name": "dian-exogena-rpa",
  "version": "0.1.0",
  "description": "Backend RPA para extraer Informacion Exogena desde MUISCA usando credenciales que el contribuyente ingresa en el momento (no se almacenan).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.47.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express-rate-limit": "^7.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
